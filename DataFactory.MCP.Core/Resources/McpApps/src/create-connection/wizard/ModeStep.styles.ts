import { CSSProperties } from "react";

export class ModeStepStyles {
  static subtitle: CSSProperties = {
    fontSize: "0.8rem",
    color: "var(--vscode-descriptionForeground, #888)",
    marginBottom: "10px",
    marginTop: 0,
  };
}
