/**
 * Styles for the FormButtons component.
 */

import { CSSProperties } from "react";

export class FormButtonsStyles {
  static container: CSSProperties = {
    display: "flex",
    gap: "8px",
    marginTop: "14px",
  };
}
