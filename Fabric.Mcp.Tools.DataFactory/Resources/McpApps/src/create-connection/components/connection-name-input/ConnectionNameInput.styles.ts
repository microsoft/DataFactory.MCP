/**
 * Styles for the ConnectionNameInput component.
 */

import { CSSProperties } from "react";

export class ConnectionNameInputStyles {
  static required: CSSProperties = {
    color: "var(--vscode-errorForeground, #f48771)",
  };
}
